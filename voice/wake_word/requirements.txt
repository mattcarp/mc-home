# Wake word detection dependencies
# Install: pip install -r requirements.txt

# Audio capture
pyaudio>=0.2.14

# Porcupine (Picovoice) — primary wake word engine (switched from openWakeWord 2026-03-15)
# v4.x: improved API, ~100ms latency, custom keyword via console.picovoice.ai
pvporcupine>=4.0.0

# openWakeWord — kept for reference; DEPRECATED for this project (see issue #9)
# Hit embedding space collapse with custom training data — Porcupine is the correct arch
openwakeword>=0.6.0

# Shared utilities
numpy>=1.24.0
